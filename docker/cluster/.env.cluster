###
# Cluster specific settings
###

## Pace-Phoenix

# Job scheduler used by cluster.
# Currently supports PBS and SLURM
CLUSTER_JOB_SCHEDULER=SLURM
# Your preferred root directory on cluster 
CLUSTER_ROOT_DIR=/storage/scratch1/4/jkamohara3/Research

# EXP_NAME=g1_loco_rigid
# EXP_NAME=g1_loco_soft
EXP_NAME=g1_mimic_gm
# EXP_NAME=g1_loco_rigid_fastsac
# EXP_NAME=g1_loco_soft_fastsac

# Docker cache dir for Isaac Sim (has to end on docker-isaac-sim)
# e.g. /cluster/scratch/$USER/docker-isaac-sim
CLUSTER_ISAACLAB_DIR=$CLUSTER_ROOT_DIR/$EXP_NAME/isaaclab
# Isaac Lab directory on the cluster (has to end on isaaclab)
# e.g. /cluster/home/$USER/isaaclab
CLUSTER_ISAAC_SIM_CACHE_DIR=$CLUSTER_ROOT_DIR/$EXP_NAME/docker-isaac-sim

# Cluster login
CLUSTER_LOGIN=pace-phoenix
# Cluster scratch directory to store the SIF file
# e.g. /cluster/scratch/$USER
CLUSTER_SIF_PATH=$CLUSTER_ROOT_DIR/isaaclabsif
# Remove the temporary isaaclab code copy after the job is done
REMOVE_CODE_COPY_AFTER_JOB=false
# Remove the temporary apptainer overlay after the job is done
REMOVE_OVERLAY_AFTER_JOB=false
# Python executable within Isaac Lab directory to run with the submitted job
CLUSTER_PYTHON_EXECUTABLE=scripts/reinforcement_learning/rsl_rl/train.py


# ### Pace-ICE

# # Job scheduler used by cluster.
# # Currently supports PBS and SLURM
# CLUSTER_JOB_SCHEDULER=SLURM
# # Your preferred root directory on cluster 
# CLUSTER_ROOT_DIR=/storage/ice1/6/4/jkamohara3/Research

# # EXP_NAME=g1_loco_rigid
# # EXP_NAME=g1_loco_soft
# # EXP_NAME=g1_loco_rigid_fastsac
# # EXP_NAME=g1_loco_soft_fastsac
# EXP_NAME=g1_mimic

# # Docker cache dir for Isaac Sim (has to end on docker-isaac-sim)
# # e.g. /cluster/scratch/$USER/docker-isaac-sim
# CLUSTER_ISAACLAB_DIR=$CLUSTER_ROOT_DIR/$EXP_NAME/isaaclab
# # Isaac Lab directory on the cluster (has to end on isaaclab)
# # e.g. /cluster/home/$USER/isaaclab
# CLUSTER_ISAAC_SIM_CACHE_DIR=$CLUSTER_ROOT_DIR/$EXP_NAME/docker-isaac-sim

# # Cluster login
# CLUSTER_LOGIN=pace-ice
# # Cluster scratch directory to store the SIF file
# # e.g. /cluster/scratch/$USER
# CLUSTER_SIF_PATH=$CLUSTER_ROOT_DIR/isaaclabsif
# # Remove the temporary isaaclab code copy after the job is done
# REMOVE_CODE_COPY_AFTER_JOB=false
# # Remove the temporary apptainer overlay after the job is done
# REMOVE_OVERLAY_AFTER_JOB=false
# # Python executable within Isaac Lab directory to run with the submitted job
# CLUSTER_PYTHON_EXECUTABLE=scripts/reinforcement_learning/rsl_rl/train.py
# # CLUSTER_PYTHON_EXECUTABLE=scripts/reinforcement_learning/holosoma_agent/train.py